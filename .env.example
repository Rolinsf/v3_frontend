# 若林轻小说：环境变量示例
# 复制本文件为 .env 并按需填写真实值。仓库不提交 .env 与任何真实凭证。

# 站点公开 URL：用于 SEO canonical、sitemap 和分享链接
NUXT_PUBLIC_SITE_URL=http://localhost:3000

# 后端 API 基础路径：正式接入真实后端时填写绝对或相对地址
NUXT_PUBLIC_API_BASE=/api

# 请求超时与重试目前由统一 useApi 客户端提供默认值；
# 后端接入后如需环境差异，再提升为 runtimeConfig，避免页面自行配置。

# 服务端 Session 密钥：仅服务端可见，生产环境必须替换为足够长的随机字符串
# NUXT_SESSION_SECRET=
